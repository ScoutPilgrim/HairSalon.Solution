@{
  Layout = "_Layout";
}
@using HairSalon.Models;
<h1>Clients</h1>

<ul>
@if (Model.Count == 0)
{
  <p>There are no clients!</p>
}
@if(Model.Count != 0)
{
  @foreach (Client client in Model)
  {
    <li>@client.GetName()</li>
  }
}
</ul>
